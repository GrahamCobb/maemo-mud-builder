Index: debian/libsoup2.2-dev.install
===================================================================
--- debian/libsoup2.2-dev.install	(revision 0)
+++ debian/libsoup2.2-dev.install	(revision 0)
@@ -0,0 +1,5 @@
+usr/include/*
+usr/lib/lib*.a
+usr/lib/lib*.so
+usr/lib/pkgconfig/*
+/usr/lib/*.la
Index: debian/libsoup2.2.dirs
===================================================================
--- debian/libsoup2.2.dirs	(revision 0)
+++ debian/libsoup2.2.dirs	(revision 0)
@@ -0,0 +1 @@
+usr/lib
Index: debian/libsoup2.2.install
===================================================================
--- debian/libsoup2.2.install	(revision 0)
+++ debian/libsoup2.2.install	(revision 0)
@@ -0,0 +1 @@
+usr/lib/lib*.so.*
Index: debian/rules
===================================================================
--- debian/rules	(revision 1)
+++ debian/rules	(working copy)
@@ -33,6 +33,7 @@
 configure-stamp:
 	dh_testdir
 	# Add here commands to configure the package.
+	REQUIRED_AUTOMAKE_VERSION=1.5 ./autogen.sh --enable-hildon --disable-cairo --host=$(DEB_HOST_GNU_TYPE) --build=$(DEB_BUILD_GNU_TYPE) --prefix=/usr --mandir=\$${prefix}/share/man --infodir=\$${prefix}/share/info
 
 	touch configure-stamp
 
@@ -74,10 +75,10 @@
 binary-arch: build install
 	dh_testdir
 	dh_testroot
-	dh_installchangelogs ChangeLog
-	dh_installdocs
-	dh_installexamples
-#	dh_install
+#	dh_installchangelogs ChangeLog
+#	dh_installdocs
+#	dh_installexamples
+	dh_install --sourcedir=debian/tmp
 #	dh_installmenu
 #	dh_installdebconf	
 #	dh_installlogrotate
@@ -87,14 +88,14 @@
 #	dh_installinit
 #	dh_installcron
 #	dh_installinfo
-	dh_installman
+#	dh_installman
 	dh_link
 	dh_strip
 	dh_compress
 	dh_fixperms
 #	dh_perl
 #	dh_python
-#	dh_makeshlibs
+	dh_makeshlibs
 	dh_installdeb
 	dh_shlibdeps
 	dh_gencontrol
Index: debian/libsoup2.2-dev.dirs
===================================================================
--- debian/libsoup2.2-dev.dirs	(revision 0)
+++ debian/libsoup2.2-dev.dirs	(revision 0)
@@ -0,0 +1,2 @@
+usr/lib
+usr/include
Index: configure.in
===================================================================
--- configure.in	(revision 1)
+++ configure.in	(working copy)
@@ -159,7 +159,7 @@
 dnl ***************
 dnl *** gtk-doc ***
 dnl ***************
-GTK_DOC_CHECK([1.0])
+dnl ** Disabled for MUD ** GTK_DOC_CHECK([1.0])
 
 
 dnl *************************************
@@ -275,6 +275,4 @@
 	libsoup/Makefile
 	tests/Makefile
 	tests/httpd.conf
-	docs/Makefile
-	docs/reference/Makefile
 	])
Index: Makefile.am
===================================================================
--- Makefile.am	(revision 1)
+++ Makefile.am	(working copy)
@@ -1,11 +1,11 @@
 ## Process this file with automake to produce Makefile.in
 AUTOMAKE_OPTIONS = 1.6
 
-SUBDIRS = libsoup tests docs
+SUBDIRS = libsoup tests 
 
 EXTRA_DIST = libsoup.pc.in gtk-doc.make libsoup-zip.in
 
-DISTCHECK_CONFIGURE_FLAGS = --enable-gtk-doc
+#DISTCHECK_CONFIGURE_FLAGS = --enable-gtk-doc
 
 pkgconfigdir = $(libdir)/pkgconfig
 
