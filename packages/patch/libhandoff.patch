Index: debian/control
===================================================================
--- debian/control	(revision 1)
+++ debian/control	(working copy)
@@ -9,9 +9,9 @@
 Architecture: any
-Depends: libhandoffBROKEN (= ${Source-Version})
+Depends: libhandoff1 (= ${Source-Version})
 Description: <insert up to 60 chars description>
  <insert long description, indented with spaces>
 
-Package: libhandoffBROKEN
+Package: libhandoff1
 Section: libs
 Architecture: any
 Depends: ${shlibs:Depends}, ${misc:Depends}
Index: debian/rules
===================================================================
--- debian/rules	(revision 1)
+++ debian/rules	(working copy)
@@ -33,7 +33,9 @@
 configure-stamp:
 	dh_testdir
 	# Add here commands to configure the package.
-
+#	intltoolize --copy --automake --force
+	autoreconf -f -i -s
+	CFLAGS="$(CFLAGS)" ./configure --host=$(DEB_HOST_GNU_TYPE) --build=$(DEB_BUILD_GNU_TYPE) --prefix=/usr --mandir=\$${prefix}/share/man --infodir=\$${prefix}/share/info --enable-hildon --disable-cairo
 	touch configure-stamp
 
 
@@ -74,10 +76,10 @@
 binary-arch: build install
 	dh_testdir
 	dh_testroot
-	dh_installchangelogs ChangeLog
-	dh_installdocs
-	dh_installexamples
-#	dh_install
+#	dh_installchangelogs ChangeLog
+#	dh_installdocs
+#	dh_installexamples
+	dh_install --sourcedir=debian/tmp
 #	dh_installmenu
 #	dh_installdebconf	
 #	dh_installlogrotate
@@ -87,14 +89,14 @@
 #	dh_installinit
 #	dh_installcron
 #	dh_installinfo
-	dh_installman
+#	dh_installman
 	dh_link
 	dh_strip
 	dh_compress
 	dh_fixperms
 #	dh_perl
 #	dh_python
-#	dh_makeshlibs
+	dh_makeshlibs -V
 	dh_installdeb
 	dh_shlibdeps
 	dh_gencontrol
Index: configure.ac
===================================================================
--- configure.ac	(revision 1)
+++ configure.ac	(working copy)
@@ -16,7 +16,7 @@
 AC_SUBST(DEPS_LIBS)
 
 #gtk-doc for documentation, currently unused
-GTK_DOC_CHECK(1.2)
+#GTK_DOC_CHECK(1.2)
 
 AC_CONFIG_FILES([Makefile libhandoff.pc])
 AC_OUTPUT
