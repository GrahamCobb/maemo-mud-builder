<?xml version="1.0"?>
<package>
  <!-- <fetch type="command">
    <command>
    pkg="libsyncml"
    date="20080204"
    release="0.35+"
    # The release should be of the form major.minor-debianversion or major.minor+.
    # The latter form means that the "svn$date" form of package version should be used
    majmin=${release%-*}
    debver=${release#*-}
    if [ "$debver" = "$release" ] ; then debver="1" ; fi
    if [ "$majmin" = "${majmin%+}" ] ; then version="$majmin+maemo-$debver" ; else version="${majmin}maemo+svn$date-1" ; fi
    pkgdir="$pkg-$version"
    branch="http://svn.opensync.org/libsyncml"
    svn export -r {${date}T0000Z} $branch/trunk $pkgdir/source
    </command>
    <depends>libopenobex1-dev, libsoup2.2-dev, libwbxml2, libbluetooth2-dev</depends>
  </fetch> -->
  <fetch type="tarball">
    <url>http://libsyncml.opensync.org/download/releases/0.4.6/libsyncml-0.4.6.tar.gz</url>
    <depends>libopenobex1-dev, libsoup2.2-dev, libwbxml2, libbluetooth2-dev</depends>
  </fetch>
  <build>
  </build>
  <deb prefix-section="0">
    <library>libsyncml0</library>
    <maintainer>Graham Cobb &lt;g+770@cobb.uk.net&gt;</maintainer>
    <description>SyncML protocol library\n Libsyncml implement the SyncML protocol. It supports SyncML version 1.0,\n 1.1 and 1.2. The available transport are Obex and Http. This library can be\n used as a client or as a server.</description>
    <build-Depends>debhelper (&gt;= 5), cmake, libopenobex1-dev, libsoup2.2-dev, libwbxml2-dev, libbluetooth2-dev</build-Depends>
  </deb>
</package>
