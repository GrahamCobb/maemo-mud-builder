<?xml version="1.0"?>
<package>
  <fetch type="command">
    <command>
    pkg="gpe-calendar"
    date="20081212"
    release="2.8+"
    # The release should be of the form major.minor-debianversion or major.minor+debianversion.
    # The latter form means that the "svn$date" form of package version should be used
    debver=${release#*-}
    if [ "$debver" = "$release" ] ; then debver=${release#*+} ; fi
    if [ "$debver" = "" ] ; then debver="1" ; fi
    majmin=${release%-*} ; version="$majmin+maemo-$debver"
    if [ "$majmin" = "$release" ] ; then majmin=${release%+*} ; version="${majmin}+maemo+svn$date-$debver" ; fi
    pkgdir="$pkg-$version"
    branch="svn://projects.linuxtogo.org/svn/gpe/trunk/base"
    svn export -r {${date}T0000Z} $branch/$pkg $pkgdir
    mv -f $pkgdir/debian/changelog $pkgdir/debian/changelog.tmp
    echo "$pkg ($version) unstable; urgency=low" &gt;$pkgdir/debian/changelog
    echo &gt;&gt;$pkgdir/debian/changelog
    echo "  * MUD build using revision {$date}" &gt;&gt;$pkgdir/debian/changelog
    echo &gt;&gt;$pkgdir/debian/changelog
    echo " -- Graham Cobb &lt;g+770@cobb.uk.net&gt;  $(date --rfc-2822)" &gt;&gt;$pkgdir/debian/changelog
    cat $pkgdir/debian/changelog.tmp &gt;&gt;$pkgdir/debian/changelog
    rm -f $pkgdir/debian/changelog.tmp
    </command>
    <depends sdk="gregale">libsoundgen, libmimedir-gnome, libgpevtype, libeventdb, libgpepimc, libgpewidget, libtododb, libxsettings-client, libhandoff, libsoup2.2-dev, gpe-icons</depends>
    <depends sdk="bora">libsoundgen, libmimedir-gnome, libgpevtype, libeventdb, libgpepimc, libgpewidget, libtododb, libxsettings-client, libhandoff, libsoup2.2-dev, gpe-icons</depends>
    <depends sdk="chinook">libsoundgen, libmimedir-gnome, libgpevtype, libeventdb, libgpepimc, libgpewidget, libtododb, libxsettings-client, libhandoff, libsoup2.2-dev, gpe-icons, intltool, libhildon1-dev, libhildonfm2-dev, libosso-dev</depends>
  </fetch>
  <build>
    <configure-append>--enable-hildon --disable-cairo --without-libschedule</configure-append>
  </build>
  <deb>
    <section>user/office</section>
    <maintainer>Graham Cobb &lt;g+770@cobb.uk.net&gt;</maintainer>
    <description>GPE PIM suite calendar application\nIncludes repeating events, alarms and import and export.\nPackaged with MUD</description>
    <depends sdk="chinook">gpe-icons (&gt;= ${source:Version}), gpesummary (&gt;= ${source:Version}), gpesyncd (&gt;= ${source:Version}), ${shlibs:Depends}, ${misc:Depends}</depends>
    <depends sdk="bora">gpe-icons (&gt;= ${Source-Version}), gpesummary (&gt;= ${Source-Version}), gpesyncd (&gt;= ${Source-Version}), ${shlibs:Depends}, ${misc:Depends}</depends>
    <depends sdk="gregale">gpe-icons (&gt;= ${Source-Version}), gpesummary (&gt;= ${Source-Version}), gpesyncd (&gt;= ${Source-Version}), ${shlibs:Depends}, ${misc:Depends}</depends>
    <build-depends sdk="chinook">debhelper (&gt;= 4), libsoundgen-dev, libmimedir-gnome-dev, libgpevtype-dev, libeventdb-dev, libgpepimc-dev, libgpewidget-dev, libtododb-dev, libxsettings-client-dev, libhandoff-dev, libsoup2.2-dev, libgnutls-dev, libgnutls13, libgcrypt11-dev, libopencdk10-dev, intltool, libhildon1-dev, libhildonfm2-dev, libosso-dev</build-depends>
    <display-name>GPE Calendar</display-name>
    <!-- <upgrade-description>Daily testing build of GPE PIM suite calendar application</upgrade-description> -->
    <upgrade-description>Updated beta of GPE PIM suite calendar application\nChanges include:\n- Support for week starting on Sunday\n- Additional import fixes\n- Fix command line import (bug 230)\n- Category changes automatically updated (bug 80)\n- Support for duplicating events\n- Translation updates\n- Change name to GPE Calendar and update menu items and icons\n- Improve day-of-week display (bug 256)</upgrade-description>
    <icon>pixmaps/gpe-calendar-26x26.png</icon>
  </deb>
</package>
